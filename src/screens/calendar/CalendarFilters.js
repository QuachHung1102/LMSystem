import React from 'react';
import {
  View,
  Text,
  useTranslations,
  useTheme,
  useActionSheet,
  ActivityIndicator,
  Button,
} from '../../core/dopebase';
import dynamicStyles from './styles';

const {width, height} = Dimensions.get('window');
